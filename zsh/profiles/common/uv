ensure_exists uv

if [[ "$1" = "python" ]]; then
  shift
  if [[ "$1" = "global" ]]; then
    shift
    command uv python install --preview-features python-install-default --default $@
  else
    command uv python $@
  fi
else
  command uv $@
fi
