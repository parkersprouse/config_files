if [[ -n $1 ]]; then
  echo "define ceil (x) {if (x<0) {return x/1} \
        else {if (scale(x)==0) {return x} \
        else {return x/1 + 1 }}} ; ceil($1)" | bc
else
  return_error 'Usage: ceil <number>'
fi
